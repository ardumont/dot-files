# wikeo
[ -d $HOME/repositories/pro/wikeo-core ] && export WIKEO_HOME=~/repositories/pro/wikeo-core

# for work
[ -f ~/work/bin/.bash_aliases-work ] && . ~/work/bin/.bash_aliases-work

# install wikeo in cluster mode
export WIKEO_DEPLOY_HOME=/home/tony/repositories/pro/wikeo-deploy

[ -f ~/work/bin/work-ssh-add.sh ] && ~/work/bin/work-ssh-add.sh

[ -f ~/work/bin/work-setup.sh ] && ~/work/bin/work-setup.sh
