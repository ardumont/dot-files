# -*- conf -*-

[general]
accounts                = ard, eniotna
pythonfile              = ~/.offlineimap-helpers.py
# ui = ttyui or blinkenlights

[Account eniotna]
localrepository         = eniotna-local
remoterepository        = eniotna-remote
# autorefresh             = 1
postsynchook            = notmuch new

[Repository eniotna-local]
type                    = Maildir
localfolders            = ~/.mails/eniotna.t/

[Repository eniotna-remote]
type                    = Gmail
remotehost              = imap.gmail.com
remoteuser              = eniotna.t@gmail.com
remotepasseval          = get_secure_credentials("~/.authinfo.gpg", "imap.gmail.com", "eniotna.t@gmail.com", "993")
realdelete              = no
ssl                     = yes
maxconnections          = 3
folderfilter            = lambda foldername: foldername not in ['[Gmail]/Spam', '[Gmail]/All Mail', '[Gmail]/Drafts', '[Gmail]/Important']
# status_backend = sqlite
# fsync = true
# holdconnectionopen = no
sslcacertfile           = /etc/ssl/certs/ca-certificates.crt
# auth_mechanisms         = GSSAPI, CRAM-MD5, XOAUTH2, PLAIN, LOGIN
auth_mechanisms         = LOGIN

[Account ard]
localrepository         = ard-local
remoterepository        = ard-remote
# autorefresh             = 1
postsynchook            = notmuch new

[Repository ard-local]
type                    = Maildir
localfolders            = ~/.mails/antoine.romain.dumont/

[Repository ard-remote]
type                    = Gmail
remotehost              = imap.gmail.com
remoteuser              = antoine.romain.dumont@gmail.com
remotepasseval          = get_secure_credentials("~/.authinfo.gpg", "imap.gmail.com", "antoine.romain.dumont@gmail.com", "993")
realdelete              = no
ssl                     = yes
maxconnections          = 3
folderfilter            = lambda foldername: foldername not in ['[Gmail]/Spam', '[Gmail]/All Mail', '[Gmail]/Drafts', '[Gmail]/Important']
# status_backend = sqlite
# fsync = true
# holdconnectionopen = no
sslcacertfile           = /etc/ssl/certs/ca-certificates.crt
auth_mechanisms         = LOGIN
